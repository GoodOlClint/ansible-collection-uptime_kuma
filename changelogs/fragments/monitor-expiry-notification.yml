minor_changes:
  - >-
    uptime_kuma_monitor - new ``expiry_notification`` option (Uptime Kuma's "Certificate Expiry Notification"
    toggle, sent as ``expiryNotification``); it has no default, so an unset option leaves a value toggled in
    the UI alone. The ``uptime_kuma`` role passes it through per monitor.
